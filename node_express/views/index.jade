html
  head
    title My first page for express
  body
    - var arreglo = [1, 2, 3, 4];
    - for (var i = arreglo.length -1; i >=0; i--)
      p= arreglo[i]
      p hola #{arreglo[i]}
        p.
          hola haz click en #[a(href="algo.com") este link ] por favor !!
    // este es un comentario


    each item in arreglo
      p= item
      case item
        when 1
          p el item es uno
        when 2
          p el item es dos
        default
          p el item es diferente de uno y dos 
